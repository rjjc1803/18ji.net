@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Layui</title>
    <meta name="renderer" content="webkit">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/layui/css/layui.css" rel="stylesheet" />
    <script src="~/layui/layui.js"></script>

    <style>
        #main {
            width: 75%;
            height: 100%;
            margin-top: 30px;
            margin-left: 150px;
            flex-wrap: nowrap; /*容器内项目换行方式*/
        }
        #top{
            color:burlywood;
            padding-bottom:20px;
            padding-top:10px;
        }
    </style>

</head>
<body>

    <div id="main">
        <div id="top">
            <label style="margin-bottom:10px;zoom:130%;">第一步：老师创建班级，并把[班级编号」告诉自己的学生。</label>
            <label style="zoom:130%;" >第二步：学生登录自己账号下的[爱考试平台」，进入[班级管理]>[加入班级」，输入老师告知的班级编号申请加入。</label>
            <label style="zoom:130%;">第三步：老师在自己账号下的[我的班级]可以查看到加入进班级的学生。</label>
        </div>
        <form class="layui-form layui-form-pane" action="">
            <div class="layui-form-item" style="margin-bottom:50px;">
                <label class="layui-form-label">班级名称</label>
                <div class="layui-input-inline">
                    <input type="text" id="ClassName" lay-verify="required" placeholder="请输入班级的名称" autocomplete="off" class="layui-input">
                </div>
            </div>
            <div class="layui-form-item layui-form-text">
                <label class="layui-form-label" style="width:500px;">班级介绍</label>
                <div class="layui-input-block">
                    <textarea id="textClass" placeholder="请输入内容" class="layui-textarea" name="desc" style="width:500px;"></textarea>
                </div>
            </div>

            <div class="site-demo-button" id="layerDemo" style="margin-bottom: 0;">
                <button id="btnAdd" type="submit" class="layui-btn" >立即创建</button>
            </div>
        </form>
    </div>

    <script>

        layui.use(['jquery', 'table', 'form'], function () {
            bindEvent();
        });

        function bindEvent(){
            $('#btnAdd').on('click', AddClass);
        }

        function AddClass() {
            if ($('#ClassName').val() == "") {
                layui.layer.msg('请输入班级名称！');
                return;
            }

            var url='/Class/AddClassByName';
            var clssObj={
                Name: $('#ClassName').val(),
                SchName:  '@ViewBag.CurUserSch',
                TeacherID: '@ViewBag.CurUserID',
                Content: $('#textClass').val()
            };

        function add(resp) {
            var obj = resp;
            if (obj.Status) {
                layui.layer.msg('添加班级成功');
               // location.href = '/Class/ClassList';
            } else {
                layui.layer.msg('添加班级失败');
            }
        };

            $.post(url,clssObj,add);
        }



    </script>
</body>
</html>
